# ~/.config/tmux/tmux.conf
Once everything has been installed, run TPM.

## Install
Clone TPM:
```bash
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

## Run
Execute:
`<tmux prefix> Ctrl+I`
